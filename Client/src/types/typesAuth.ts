export interface registerType {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
  shopName: string;
}

export interface loginType {
  email: string;
  password: string;
}
